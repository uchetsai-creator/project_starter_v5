# Distribution

## Package Details

| Field | Value |
|---|---|
| Package name | mytool |
| Registry | PyPI |
| Install | `pip install mytool` |

## Publish

Run `twine upload dist/*` after building with `python -m build`.
