# CLI Contract

## build

Build the project for distribution.

Options: `--output PATH`, `--format FORMAT`

Usage: `mytool build --output dist/ --format wheel`

## deploy

Deploy the built artifact to the target environment.

Options: `--env ENV`, `--dry-run`

## test

Run the project test suite.

Flags: `--verbose`, `-v`, `--coverage`
