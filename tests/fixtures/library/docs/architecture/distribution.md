# Distribution

## Package Details

| Field | Value |
|---|---|
| Package name | mylib |
| Registry | PyPI |
| Install | `pip install mylib` |

## Publish

Run `pip install build twine && python -m build && twine upload dist/*`.
